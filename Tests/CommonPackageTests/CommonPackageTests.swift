    import XCTest
    @testable import CommonPackage

    final class CommonPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(CommonPackage().text, "Hello, World!")
        }
    }
